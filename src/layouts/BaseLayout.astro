---
import { ClientRouter } from 'astro:transitions';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = "Aishwarya Srivastava is a Senior UX/UI & Product Designer in Bengaluru with 5+ years across enterprise SaaS, telecom, cloud, healthcare, logistics, and CRM.",
} = Astro.props;

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="icon" href={`${base}/favicon.svg`} type="image/svg+xml" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Caveat:wght@500;600;700&family=Inter:wght@400;500;600;700&family=Instrument+Serif:ital@0;1&family=JetBrains+Mono:wght@400;500&display=swap"
      rel="stylesheet"
    />
    <ClientRouter />
  </head>
  <body>
    <div class="cursor-blob" id="blob" aria-hidden="true"></div>

    <header class="nav wrap">
      <a class="brand" href={`${base}/`}>
        <span class="brand-mark"></span> Aishwarya Srivastava
      </a>
      <nav class="nav-links">
        <a href={`${base}/#about`}>About</a>
        <a href={`${base}/#work`}>Work</a>
        <a href={`${base}/#process`}>Process</a>
        <a href={`${base}/#contact`}>Contact</a>
      </nav>
      <div class="nav-right">
        <button class="theme-toggle" id="theme-toggle" aria-label="Toggle day and night mode">
          <span class="tt-sun">☀</span>
          <span class="tt-moon">☾</span>
        </button>
        <a class="resume-pill" href={`${base}/Aishwarya-Srivastava-Resume.pdf`} target="_blank" rel="noopener">Résumé ↗</a>
        <button class="nav-toggle" id="nav-toggle" aria-label="Open menu" aria-expanded="false" aria-controls="mobile-menu">
          <span></span><span></span><span></span>
        </button>
      </div>
    </header>

    <div class="mobile-menu" id="mobile-menu">
      <a href={`${base}/#about`}>About</a>
      <a href={`${base}/#work`}>Work</a>
      <a href={`${base}/#process`}>Process</a>
      <a href={`${base}/#contact`}>Contact</a>
      <a href={`${base}/Aishwarya-Srivastava-Resume.pdf`} target="_blank" rel="noopener">Résumé ↗</a>
    </div>

    <!-- floating nav dock: appears after the banner, scroll-spies sections -->
    <nav class="dock" id="dock" aria-label="Section navigation">
      <a href={`${base}/#top`} data-target="top"><span class="dot"></span> Home</a>
      <a href={`${base}/#about`} data-target="about"><span class="dot"></span> About</a>
      <a href={`${base}/#work`} data-target="work"><span class="dot"></span> Work</a>
      <a href={`${base}/#contact`} data-target="contact"><span class="dot"></span> Contact</a>
    </nav>

    <main>
      <slot />
    </main>

    <footer class="site-foot wrap">
      <div class="foot-cols">
        <div>
          <p class="foot-name">Aishwarya Srivastava</p>
          <p class="foot-sub">Product designer, Bengaluru, IST</p>
        </div>
        <ul class="foot-nav">
          <li><a href={`${base}/#work`}>Work</a></li>
          <li><a href={`${base}/#about`}>About</a></li>
          <li><a href={`${base}/#contact`}>Contact</a></li>
        </ul>
      </div>
      <p class="foot-meta">
        © <span id="year"></span>, Aishwarya Srivastava. Handmade in Bengaluru.
      </p>
    </footer>

    <style>
      .nav {
        position: sticky;
        top: 0;
        z-index: 100;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        min-height: var(--nav-h);
        padding-block: 1.1rem;
        /* transparent by default so it floats over the banner sky */
        background: transparent;
        border-bottom: 1px solid transparent;
        transition: background 0.35s var(--ease), border-color 0.35s var(--ease),
          backdrop-filter 0.35s var(--ease);
      }
      /* solid, blurred backdrop once scrolled past the banner */
      .nav.scrolled {
        backdrop-filter: saturate(1.4) blur(10px);
        background: color-mix(in srgb, var(--bg) 82%, transparent);
        border-bottom: 1px solid var(--line);
      }
      .brand {
        display: inline-flex;
        align-items: center;
        gap: 0.6rem;
        font-weight: 600;
        letter-spacing: -0.01em;
      }
      .brand-mark {
        width: 12px;
        height: 12px;
        border-radius: 4px;
        background: var(--accent);
        transform: rotate(12deg);
        transition: transform 0.4s var(--ease);
      }
      .brand:hover .brand-mark {
        transform: rotate(120deg) scale(1.2);
      }
      .nav-links {
        display: flex;
        gap: 1.5rem;
        font-size: 0.95rem;
      }
      .nav-links a {
        position: relative;
        color: var(--ink-soft);
        transition: color 0.2s var(--ease);
      }
      .nav-links a::after {
        content: "";
        position: absolute;
        left: 0;
        bottom: -4px;
        width: 100%;
        height: 2px;
        background: var(--accent);
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.3s var(--ease);
      }
      .nav-links a:hover {
        color: var(--ink);
      }
      .nav-links a:hover::after {
        transform: scaleX(1);
      }
      .nav-right {
        display: flex;
        align-items: center;
        gap: 0.85rem;
      }
      .theme-toggle {
        appearance: none;
        border: 1.5px solid var(--line);
        background: var(--card);
        color: var(--fg);
        width: 40px;
        height: 40px;
        border-radius: 50%;
        cursor: pointer;
        font-size: 0.95rem;
        display: grid;
        place-items: center;
        transition: transform 0.3s var(--ease), border-color 0.3s var(--ease);
      }
      .theme-toggle:hover {
        transform: rotate(25deg) scale(1.08);
        border-color: var(--accent);
      }
      .tt-moon {
        display: none;
      }
      :global([data-theme="dark"]) .tt-sun {
        display: none;
      }
      :global([data-theme="dark"]) .tt-moon {
        display: inline;
      }
      .resume-pill {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        font-size: 0.85rem;
        font-weight: 500;
        color: var(--paper);
        background: var(--accent);
        border: 1px solid var(--accent);
        border-radius: 999px;
        padding: 0.5rem 1rem;
        transition: transform 0.25s var(--ease), background 0.25s var(--ease),
          box-shadow 0.25s var(--ease);
      }
      .resume-pill:hover {
        transform: translateY(-2px);
        box-shadow: 0 10px 24px -12px var(--accent);
        background: color-mix(in srgb, var(--accent) 88%, #000);
      }
      .dot-live {
        width: 8px;
        height: 8px;
        border-radius: 50%;
        background: var(--accent-3);
        box-shadow: 0 0 0 0 color-mix(in srgb, var(--accent-3) 60%, transparent);
        animation: pulse 2s infinite;
      }
      @keyframes pulse {
        0% {
          box-shadow: 0 0 0 0 color-mix(in srgb, var(--accent-3) 60%, transparent);
        }
        70% {
          box-shadow: 0 0 0 8px transparent;
        }
        100% {
          box-shadow: 0 0 0 0 transparent;
        }
      }
      /* ---- floating nav dock ---- */
      .dock {
        position: fixed;
        right: 1.25rem;
        top: 50%;
        transform: translateY(-50%) translateX(20px);
        z-index: 90;
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
        padding: 0.6rem 0.7rem;
        border-radius: 16px;
        background: color-mix(in srgb, var(--card) 72%, transparent);
        border: 1px solid var(--line);
        backdrop-filter: saturate(1.3) blur(10px);
        box-shadow: 0 18px 40px -24px rgba(20, 40, 70, 0.4);
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.4s var(--ease), transform 0.4s var(--ease);
      }
      .dock.show {
        opacity: 1;
        pointer-events: auto;
        transform: translateY(-50%) translateX(0);
      }
      .dock a {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-family: var(--font-mono);
        font-size: 0.68rem;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        color: var(--ink-faint);
        padding: 0.35rem 0.5rem;
        border-radius: 9px;
        transition: color 0.25s var(--ease), background 0.25s var(--ease);
      }
      .dock a .dot {
        width: 6px;
        height: 6px;
        border-radius: 50%;
        background: var(--ink-faint);
        transition: background 0.25s var(--ease), transform 0.25s var(--ease);
      }
      .dock a:hover {
        color: var(--ink);
      }
      .dock a.active {
        color: var(--ink);
        background: color-mix(in srgb, var(--accent) 12%, transparent);
      }
      .dock a.active .dot {
        background: var(--accent);
        transform: scale(1.5);
      }
      @media (max-width: 720px) {
        .dock { display: none; }
      }

      .site-foot {
        margin-top: 8rem;
        padding-block: 3rem 4rem;
        border-top: 1px solid var(--line);
      }
      .foot-cols {
        display: flex;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 2rem;
      }
      .foot-name {
        font-family: var(--font-serif);
        font-size: 1.6rem;
      }
      .foot-sub {
        color: var(--ink-faint);
        font-size: 0.9rem;
      }
      .foot-nav {
        display: flex;
        gap: 1.5rem;
        list-style: none;
        margin: 0;
        padding: 0;
        font-size: 0.95rem;
      }
      .foot-nav a:hover {
        color: var(--accent);
      }
      .foot-meta {
        margin-top: 2.5rem;
        font-family: var(--font-mono);
        font-size: 0.75rem;
        color: var(--ink-faint);
      }
      /* ---- hamburger + mobile menu ---- */
      .nav-toggle {
        display: none;
        flex-direction: column;
        justify-content: center;
        gap: 5px;
        width: 42px;
        height: 42px;
        padding: 0 9px;
        border: 1.5px solid var(--line);
        border-radius: 12px;
        background: var(--card);
        cursor: pointer;
      }
      .nav-toggle span {
        display: block;
        height: 2px;
        width: 100%;
        background: var(--fg);
        border-radius: 2px;
        transition: transform 0.3s var(--ease), opacity 0.2s var(--ease);
      }
      .nav-toggle[aria-expanded='true'] span:nth-child(1) {
        transform: translateY(7px) rotate(45deg);
      }
      .nav-toggle[aria-expanded='true'] span:nth-child(2) { opacity: 0; }
      .nav-toggle[aria-expanded='true'] span:nth-child(3) {
        transform: translateY(-7px) rotate(-45deg);
      }
      .mobile-menu {
        position: fixed;
        top: var(--nav-h);
        left: 0;
        right: 0;
        z-index: 95;
        display: flex;
        flex-direction: column;
        gap: 0.25rem;
        padding: 1rem var(--gutter) 1.5rem;
        background: color-mix(in srgb, var(--bg) 96%, transparent);
        backdrop-filter: saturate(1.4) blur(12px);
        border-bottom: 1px solid var(--line);
        transform: translateY(-12px);
        opacity: 0;
        visibility: hidden;
        transition: opacity 0.3s var(--ease), transform 0.3s var(--ease),
          visibility 0.3s;
      }
      .mobile-menu.open {
        transform: translateY(0);
        opacity: 1;
        visibility: visible;
      }
      .mobile-menu a {
        padding: 0.85rem 0.25rem;
        font-size: 1.15rem;
        color: var(--ink);
        border-bottom: 1px solid var(--line);
      }
      .mobile-menu a:last-child {
        border-bottom: 0;
        color: var(--accent);
        font-weight: 600;
      }

      @media (max-width: 720px) {
        .nav-links { display: none; }
        .resume-pill { display: none; }
        .nav-toggle { display: flex; }
      }
      @media (min-width: 721px) {
        .mobile-menu { display: none; }
      }
    </style>

    <script>
      // Runs on first load and after every View Transition navigation.
      function initTheme() {
        // default to dark; respect the visitor's explicit choice once they toggle
        const saved = localStorage.getItem('theme');
        const theme = saved || 'dark';
        document.documentElement.setAttribute('data-theme', theme);
      }
      initTheme();

      function wireUp() {
        // year
        const y = document.getElementById('year');
        if (y) y.textContent = String(new Date().getFullYear());

        // theme toggle
        const toggle = document.getElementById('theme-toggle');
        if (toggle && !toggle.dataset.wired) {
          toggle.dataset.wired = '1';
          toggle.addEventListener('click', () => {
            const cur = document.documentElement.getAttribute('data-theme');
            const next = cur === 'dark' ? 'light' : 'dark';
            document.documentElement.setAttribute('data-theme', next);
            localStorage.setItem('theme', next);
          });
        }

        // mobile hamburger menu
        const navToggle = document.getElementById('nav-toggle');
        const mobileMenu = document.getElementById('mobile-menu');
        if (navToggle && mobileMenu && !navToggle.dataset.wired) {
          navToggle.dataset.wired = '1';
          const setOpen = (open: boolean) => {
            mobileMenu.classList.toggle('open', open);
            navToggle.setAttribute('aria-expanded', open ? 'true' : 'false');
          };
          navToggle.addEventListener('click', () =>
            setOpen(!mobileMenu.classList.contains('open'))
          );
          mobileMenu.querySelectorAll('a').forEach((a) =>
            a.addEventListener('click', () => setOpen(false))
          );
          window.addEventListener('resize', () => {
            if (window.innerWidth > 720) setOpen(false);
          });
        }

        // topbar backdrop + floating dock visibility + scroll-spy
        const nav = document.querySelector<HTMLElement>('.nav');
        const dock = document.querySelector<HTMLElement>('.dock');
        const dockLinks = dock ? [...dock.querySelectorAll<HTMLElement>('a')] : [];
        if (nav && !nav.dataset.wired) {
          nav.dataset.wired = '1';
          const spy = () => {
            const y = window.scrollY;
            nav.classList.toggle('scrolled', y > 40);
            if (dock) dock.classList.toggle('show', y > window.innerHeight * 0.6);
            // active section = last section whose top passed the 40% line
            let current = 'top';
            for (const link of dockLinks) {
              const id = link.dataset.target || '';
              const el = document.getElementById(id);
              if (el && el.getBoundingClientRect().top <= window.innerHeight * 0.4) {
                current = id;
              }
            }
            dockLinks.forEach((l) =>
              l.classList.toggle('active', l.dataset.target === current)
            );
          };
          window.addEventListener('scroll', spy, { passive: true });
          spy();
        }

        // reveal on scroll — IntersectionObserver is progressive enhancement.
        const io = new IntersectionObserver(
          (entries) => {
            entries.forEach((e) => {
              if (e.isIntersecting) {
                e.target.classList.add('in');
                io.unobserve(e.target);
              }
            });
          },
          { threshold: 0.12, rootMargin: '0px 0px -8% 0px' }
        );
        // Fallback: reveal anything already within the viewport using layout
        // geometry. IntersectionObserver does not fire while a document is
        // hidden (e.g. opened in a background tab), so this guarantees
        // above-the-fold content is never stuck invisible.
        const revealInView = () => {
          const vh = window.innerHeight || document.documentElement.clientHeight;
          document.querySelectorAll('.reveal:not(.in)').forEach((el) => {
            const r = el.getBoundingClientRect();
            if (r.top < vh * 0.92 && r.bottom > 0) el.classList.add('in');
          });
        };
        document.querySelectorAll('.reveal:not(.in)').forEach((el) => io.observe(el));
        revealInView();
        // Re-check when the tab becomes visible (IO may have been dormant).
        document.addEventListener('visibilitychange', () => {
          if (!document.hidden) revealInView();
        });
        window.addEventListener('load', revealInView, { once: true });

        // cursor blob (pointer devices only)
        const blob = document.getElementById('blob');
        if (blob && window.matchMedia('(hover: hover)').matches && !blob.dataset.wired) {
          blob.dataset.wired = '1';
          let x = window.innerWidth / 2,
            y2 = window.innerHeight / 2,
            tx = x,
            ty = y2;
          window.addEventListener('mousemove', (e) => {
            tx = e.clientX;
            ty = e.clientY;
            blob.style.opacity = '0.85';
          });
          const loop = () => {
            x += (tx - x) * 0.15;
            y2 += (ty - y2) * 0.15;
            blob.style.transform = `translate(${x}px, ${y2}px) translate(-50%, -50%)`;
            requestAnimationFrame(loop);
          };
          loop();
          document.addEventListener('mouseover', (e) => {
            const t = e.target as HTMLElement;
            const hot = t.closest('a, button, .project-card');
            blob.classList.toggle('hot', !!hot);
          });
        }

        // magnetic buttons
        document.querySelectorAll<HTMLElement>('.magnetic').forEach((el) => {
          if (el.dataset.wired) return;
          el.dataset.wired = '1';
          el.addEventListener('mousemove', (e) => {
            const r = el.getBoundingClientRect();
            const mx = e.clientX - r.left - r.width / 2;
            const my = e.clientY - r.top - r.height / 2;
            el.style.transform = `translate(${mx * 0.25}px, ${my * 0.35}px)`;
          });
          el.addEventListener('mouseleave', () => {
            el.style.transform = '';
          });
        });
      }

      wireUp();
      document.addEventListener('astro:after-swap', () => {
        initTheme();
        wireUp();
      });
      document.addEventListener('astro:page-load', wireUp);
    </script>
  </body>
</html>
